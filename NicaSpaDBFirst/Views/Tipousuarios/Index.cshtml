@model IEnumerable<NicaSpaDBFirst.Models.Tipousuario>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Tipousuario1)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Tipousuario1)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Codigotipousuario">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Codigotipousuario">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Codigotipousuario">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
